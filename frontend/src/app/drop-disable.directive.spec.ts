import { DropDisableDirective } from './drop-disable.directive';

describe('DropDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDisableDirective();
    expect(directive).toBeTruthy();
  });
});
